// Code generated by goenums v0.4.9; DO NOT EDIT.
//
// github.com/zarldev/goenums
//
// using the command:
// goenums smart_mode.go

package dto

import (
	"bytes"
	"database/sql/driver"
	"fmt"
	"iter"
	"math"

	"golang.org/x/exp/constraints"
)

// SmartMode is a type that represents a single enum value.
// It combines the core information about the enum constant and it's defined fields.
type SmartMode struct {
	smartMode
}

// smartModesContainer is the container for all enum values.
// It is private and should not be used directly use the public methods on the SmartMode type.
type smartModesContainer struct {
	SMARTMODENONE   SmartMode
	SMARTMODELEGACY SmartMode
	SMARTMODEDIRECT SmartMode
}

// SmartModes is a main entry point using the SmartMode type.
// It it a container for all enum values and provides a convenient way to access all enum values and perform
// operations, with convenience methods for common use cases.
var SmartModes = smartModesContainer{
	SMARTMODENONE: SmartMode{
		smartMode: SmartModeNone,
	},
	SMARTMODELEGACY: SmartMode{
		smartMode: SmartModeLegacy,
	},
	SMARTMODEDIRECT: SmartMode{
		smartMode: SmartModeDirect,
	},
}

// invalidSmartMode is an invalid sentinel value for SmartMode
var invalidSmartMode = SmartMode{
	smartMode: -1,
}

// allSlice returns a slice of all enum values.
// This method is useful for iterating over all enum values in a loop.
func (s smartModesContainer) allSlice() []SmartMode {
	return []SmartMode{
		SmartModes.SMARTMODENONE,
		SmartModes.SMARTMODELEGACY,
		SmartModes.SMARTMODEDIRECT,
	}
}

// All returns an iterator over all enum values.
// This method is useful for iterating over all enum values in a loop.
func (s smartModesContainer) All() iter.Seq[SmartMode] {
	return func(yield func(SmartMode) bool) {
		for _, v := range s.allSlice() {
			if !yield(v) {
				return
			}
		}
	}
}

// ParseSmartMode parses the input value into an enum value.
// It returns the parsed enum value or an error if the input is invalid.
// It is a convenience function that can be used to parse enum values from
// various input types, such as strings, byte slices, or other enum types.
func ParseSmartMode(input any) (SmartMode, error) {
	switch v := input.(type) {
	case SmartMode:
		return v, nil
	case string:
		if result := stringToSmartMode(v); result != nil {
			return *result, nil
		}
	case fmt.Stringer:
		if result := stringToSmartMode(v.String()); result != nil {
			return *result, nil
		}
	case []byte:
		if result := stringToSmartMode(string(v)); result != nil {
			return *result, nil
		}
	case int:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case int8:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case int16:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case int32:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case int64:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case uint:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case uint8:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case uint16:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case uint32:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case uint64:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case float32:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	case float64:
		if result := numberToSmartMode(v); result != nil {
			return *result, nil
		}
	default:
		return invalidSmartMode, fmt.Errorf("invalid type %T", input)
	}
	return invalidSmartMode, nil
}

// smartModesNameMap is a map of enum values to their SmartMode representation
// It is used to convert string representations of enum values into their SmartMode representation.
var smartModesNameMap = map[string]SmartMode{
	"none":   SmartModes.SMARTMODENONE,
	"legacy": SmartModes.SMARTMODELEGACY,
	"direct": SmartModes.SMARTMODEDIRECT,
}

// stringToSmartMode converts a string representation of an enum value into its SmartMode representation
// It returns a pointer to the SmartMode representation of the enum value if the string is valid
// Otherwise, it returns nil
func stringToSmartMode(s string) *SmartMode {
	if t, ok := smartModesNameMap[s]; ok {
		return &t
	}
	return nil
}

// numberToSmartMode converts a numeric value to a SmartMode
// It returns a pointer to the SmartMode representation of the enum value if the numeric value is valid
// Otherwise, it returns nil
func numberToSmartMode[T constraints.Integer | constraints.Float](num T) *SmartMode {
	f := float64(num)
	if math.Floor(f) != f {
		return nil
	}
	i := int(f)
	if i <= 0 || i > len(SmartModes.allSlice()) {
		return nil
	}
	result := SmartModes.allSlice()[i-1]
	if !result.IsValid() {
		return nil
	}
	return &result
}

// ExhaustiveSmartModes iterates over all enum values and calls the provided function for each value.
// This function is useful for performing operations on all valid enum values in a loop.
func ExhaustiveSmartModes(f func(SmartMode)) {
	for _, p := range SmartModes.allSlice() {
		f(p)
	}
}

// validSmartModes is a map of enum values to their validity
var validSmartModes = map[SmartMode]bool{
	SmartModes.SMARTMODENONE:   true,
	SmartModes.SMARTMODELEGACY: true,
	SmartModes.SMARTMODEDIRECT: true,
}

// IsValid checks whether the SmartModes value is valid.
// A valid value is one that is defined in the original enum and not marked as invalid.
func (s SmartMode) IsValid() bool {
	return validSmartModes[s]
}

// MarshalJSON implements the json.Marshaler interface for SmartMode.
// It returns the JSON representation of the enum value as a byte slice.
func (s SmartMode) MarshalJSON() ([]byte, error) {
	return []byte("\"" + s.String() + "\""), nil
}

// UnmarshalJSON implements the json.Unmarshaler interface for SmartMode.
// It parses the JSON representation of the enum value from the byte slice.
// It returns an error if the input is not a valid JSON representation.
func (s *SmartMode) UnmarshalJSON(by []byte) error {
	by = bytes.Trim(bytes.Trim(by, "\""), "\"")
	news, err := ParseSmartMode(by)
	if err != nil {
		return err
	}
	*s = news
	return nil
}

// MarshalText implements the encoding.TextMarshaler interface for SmartMode.
// It returns the string representation of the enum value as a byte slice
func (s SmartMode) MarshalText() ([]byte, error) {
	return []byte("\"" + s.String() + "\""), nil
}

// UnmarshalText implements the encoding.TextUnmarshaler interface for SmartMode.
// It parses the string representation of the enum value from the byte slice.
// It returns an error if the byte slice does not contain a valid enum value.
func (s *SmartMode) UnmarshalText(by []byte) error {
	news, err := ParseSmartMode(by)
	if err != nil {
		return err
	}
	*s = news
	return nil
}

// Scan implements the database/sql.Scanner interface for SmartMode.
// It parses the string representation of the enum value from the database row.
// It returns an error if the row does not contain a valid enum value.
func (s *SmartMode) Scan(value any) error {
	news, err := ParseSmartMode(value)
	if err != nil {
		return err
	}
	*s = news
	return nil
}

// Value implements the database/sql/driver.Valuer interface for SmartMode.
// It returns the string representation of the enum value.
func (s SmartMode) Value() (driver.Value, error) {
	return s.String(), nil
}

// MarshalBinary implements the encoding.BinaryMarshaler interface for SmartMode.
// It returns the binary representation of the enum value as a byte slice.
func (s SmartMode) MarshalBinary() ([]byte, error) {
	return []byte("\"" + s.String() + "\""), nil
}

// UnmarshalBinary implements the encoding.BinaryUnmarshaler interface for SmartMode.
// It parses the binary representation of the enum value from the byte slice.
// It returns an error if the byte slice does not contain a valid enum value.
func (s *SmartMode) UnmarshalBinary(by []byte) error {
	news, err := ParseSmartMode(by)
	if err != nil {
		return err
	}
	*s = news
	return nil
}

// MarshalYAML implements the yaml.Marshaler interface for SmartMode.
// It returns the string representation of the enum value.
func (s SmartMode) MarshalYAML() ([]byte, error) {
	return []byte(s.String()), nil
}

// UnmarshalYAML implements the yaml.Unmarshaler interface for Planet.
// It parses the byte slice representation of the enum value and returns an error
// if the YAML byte slice does not contain a valid enum value.
func (s *SmartMode) UnmarshalYAML(by []byte) error {
	news, err := ParseSmartMode(by)
	if err != nil {
		return err
	}
	*s = news
	return nil
}

// smartmodeNames is a constant string slice containing all enum values cononical absolute names
const smartmodeNames = "nonelegacydirect"

// smartmodeNamesMap is a map of enum values to their canonical absolute
// name positions within the smartmodeNames string slice
var smartmodeNamesMap = map[SmartMode]string{
	SmartModes.SMARTMODENONE:   smartmodeNames[0:4],
	SmartModes.SMARTMODELEGACY: smartmodeNames[4:10],
	SmartModes.SMARTMODEDIRECT: smartmodeNames[10:16],
}

// String implements the Stringer interface.
// It returns the canonical absolute name of the enum value.
func (s SmartMode) String() string {
	if str, ok := smartmodeNamesMap[s]; ok {
		return str
	}
	return fmt.Sprintf("smartmode(%d)", s.smartMode)
}

// Compile-time check that all enum values are valid.
// This function is used to ensure that all enum values are defined and valid.
// It is called by the compiler to verify that the enum values are valid.
func _() {
	// An "invalid array index" compiler error signifies that the constant values have changed.
	// Re-run the goenums command to generate them again.
	// Does not identify newly added constant values unless order changes
	var x [3]struct{}
	_ = x[SmartModeNone]
	_ = x[SmartModeLegacy-1]
	_ = x[SmartModeDirect-2]
}
